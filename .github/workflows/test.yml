name: Django Test

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      db:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: movie_data
          MYSQL_USER: selina
          MYSQL_PASSWORD: snowBall
          MYSQL_ROOT_PASSWORD: snowBall2024
        ports:
          - 3306:3306

    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r code/requirements.txt

      - name: Run migrations
        env:
          DB_HOST: 127.0.0.1
          DB_NAME: movie_data
          DB_USER: selina
          DB_PASSWORD: snowBall
        run: python code/my_project/manage.py migrate

      - name: Run tests
        env:
          DB_HOST: 127.0.0.1
          DB_NAME: movie_data
          DB_USER: selina
          DB_PASSWORD: snowBall
        run: python code/my_project/manage.py test
